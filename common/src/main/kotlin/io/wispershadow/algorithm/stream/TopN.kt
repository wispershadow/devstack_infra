package io.wispershadow.algorithm.stream

object TopN {
    fun find(valueList: List<Comparable<*>>, nth: Int) {

    }

    private fun findMediumAsPivot(valueList: List<Comparable<*>>): Int {
        return 0
    }
}